{{- range $i, $_ := until (int .Values.numCacheServers) }}
apiVersion: v1
kind: Service
metadata:
  name: cache{{ $i }}
spec:
  selector:
    app: cache{{ $i }}
  ports:
    - port: 80
---
{{- end }}
